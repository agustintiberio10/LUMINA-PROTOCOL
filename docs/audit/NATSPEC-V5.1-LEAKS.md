# NatSpec V5.1 Alignment Review

> 📋 **Review-only inventory.** This document lists Solidity NatSpec comments that may benefit from V5.1 alignment edits. **No code logic is affected** — only documentation comments.
>
> **Decision required from founder before applying.** NatSpec is part of contract metadata, so even comment-only edits trigger recompilation. Apply in a separate sprint with full Foundry test re-run after each edit.

## Findings

### Cosmetic — version label drift (1 finding)

`src/token/LuminaTokenV2.sol:14`
```solidity
/// @dev Distribution V5.0: 70% BondVault | 14% CEX Reserve | 8% Founder | 5% LBP | 3% Treasury
```
**Issue**: header says **V5.0** but the constructor at lines 71-75 mints the V5.1 distribution (70/14/8/5/3 = 100M, verified in audit). The numbers are correct; only the version label is one minor revision behind.

**Suggested edit**:
```solidity
/// @dev Distribution V5.1: 70% BondVault | 14% CEX Reserve | 8% Founder | 5% LBP | 3% Treasury
```

**Risk**: zero functional impact. Triggers recompile (NatSpec is in metadata).

## Findings ruled OUT (verified false positives)

The audit grep flagged 9 NatSpec hits. After manual review, **8 are false positives** matching benign substrings:

- `BondVault.sol:14` — "LUMINA tokens are NOT locked or reserved — they sit passively" (matched on "passively")
- `BondVault.sol:124,145,146,244,324,340,341` — phrases like "to this contract", "ERC-1155 tokens accidentally sent", "Release a reservation when a policy expires", "INTEGER DOLLARS" — all benign

None of these reference V4 vaults, AltSeasonVesting, EARN, or aUSDC deposits.

## Recommendation

1. **Defer to a dedicated NatSpec sprint** — bundle this 1 cosmetic change with any other comment-only fixes that surface over time, then run `forge build && forge test` to confirm no regressions.
2. **Do NOT** edit in this docs alignment PR (`chore/v5.1-full-alignment`) — keeping the scope strictly to .md / .txt / .json files preserves the "zero compilation impact" property.

## Cross-reference

- Tokenomics canonical (verified): `src/token/LuminaTokenV2.sol:71-75`
- Architecture overview: [`SECURITY.md`](../../SECURITY.md)
- Aave role: [`docs/architecture/AAVE-INTEGRATION.md`](../architecture/AAVE-INTEGRATION.md)

---

_Generated by full-alignment audit — `chore/v5.1-full-alignment` sprint, 2026-05-04._
