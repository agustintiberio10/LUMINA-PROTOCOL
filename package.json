{
  "name": "lumina-protocol",
  "version": "2.0.0",
  "description": "Lumina Protocol — Parametric Insurance on Base L2",
  "scripts": {
    "start": "node api/server.js",
    "dev": "node api/server.js",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network baseMainnet",
    "deploy:autoresolver": "npx hardhat run scripts/deploy-autoresolver.js --network baseMainnet",
    "test": "npx hardhat test"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "better-sqlite3": "^12.6.2",
    "dotenv": "^16.6.1",
    "ethers": "^6.16.0",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
